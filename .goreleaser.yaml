# https://goreleaser.com/customization/
project_name: iwaki-ai

before:
  hooks:
    - go mod tidy

builds:
  - id: bot
    binary: iwaki-ai-bot
    main: ./cmd/bot
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

archives:
  - id: bot
    builds:
      - bot
    name_template: >-
      {{ .ProjectName }}-bot_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format: tar.gz
    files:
      - LICENSE*
      - README*

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^chore:'
      - '^test:'
      - 'merge conflict'
      - Merge pull request
      - Merge remote-tracking branch
      - Merge branch
