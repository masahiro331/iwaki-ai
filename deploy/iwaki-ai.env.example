# Copy to /etc/iwaki-ai/iwaki-ai.env on the host and fill in real values.
# File should be owned by root:iwaki-ai with mode 0640 so secrets don't
# leak to other accounts on the box.

DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=
GEMINI_API_KEY=
